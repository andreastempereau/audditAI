{
  "name": "audditai-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd crossaudit/web && npm run dev",
    "build": "cd crossaudit/web && npm install && npm run build",
    "start": "cd crossaudit/web && npm run start"
  }
}